Destinations Index

<table style="width:100%">
<tbody>
  <% @destinations.each do |destination| %>
    <tr>
        <td><%= link_to "Go on #{attraction.name}", attraction_path(attraction) %></td> 
        <% end %>  
    </tr> 
  <% end %>
</tbody>
</table>

<% if current_user.admin %>
<td><%= link_to "New Attraction", new_attraction_path(@attraction) %></td>
<% end %>